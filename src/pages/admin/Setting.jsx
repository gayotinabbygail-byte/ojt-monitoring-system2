import { useState } from "react";
import { Bell, Check, Download, LockKeyhole, LogOut, Moon, Palette, Save, ShieldCheck, Sun, UserCircle, X } from "lucide-react";

const tabs = [["profile", "Profile", UserCircle], ["account", "Account", LockKeyhole], ["notifications", "Notifications", Bell], ["preferences", "Preferences", Palette], ["security", "Security & privacy", ShieldCheck]];
const notificationItems = ["Attendance Notifications", "OJT Hours Notifications", "Evaluation Notifications", "Report Notifications", "System Announcements", "Email Notifications"];
const styles = `.settings-page{color:#172033;padding-bottom:28px}.settings-heading{margin-bottom:22px}.settings-heading h1{margin:0 0 7px;font-size:clamp(28px,3vw,38px)}.settings-heading p{margin:0;color:#718096;font-size:14px}.settings-layout{display:grid;grid-template-columns:210px 1fr;gap:20px}.settings-tabs,.settings-card{border:1px solid #e3e9f1;border-radius:10px;background:#fff;box-shadow:0 7px 22px #18263f0a}.settings-tabs{display:grid;align-content:start;gap:5px;padding:8px}.settings-tab{display:flex;align-items:center;gap:9px;padding:11px;border:0;border-radius:7px;background:transparent;color:#718096;font-weight:800;text-align:left;cursor:pointer}.settings-tab.active,.settings-tab:hover{background:#eaf2ff;color:#2868c7}.settings-content{display:grid;gap:16px}.settings-card{padding:22px}.settings-card h2{margin:0 0 5px;font-size:17px}.settings-card-heading{display:flex;justify-content:space-between;margin-bottom:18px}.settings-card-heading p,.settings-row p{margin:0;color:#718096;font-size:12px}.settings-profile{display:flex;align-items:center;gap:15px;padding-bottom:18px;border-bottom:1px solid #edf0f4}.settings-avatar{display:grid;place-items:center;width:62px;height:62px;border-radius:17px;background:#e6efff;color:#2868c7;font-size:20px;font-weight:800}.settings-profile strong{display:block;font-size:17px}.settings-profile span{color:#718096;font-size:12px}.settings-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:14px;margin-top:18px}.settings-field{display:grid;gap:6px;color:#718096;font-size:11px;font-weight:800}.settings-field input{box-sizing:border-box;width:100%;padding:10px;border:1px solid #dbe3ed;border-radius:7px;background:#fff;color:#172033}.settings-row{display:flex;align-items:center;justify-content:space-between;gap:14px;padding:14px 0;border-bottom:1px solid #edf0f4}.settings-row:last-child{border:0}.settings-row strong{display:block;margin-bottom:3px;font-size:13px}.settings-toggle{width:39px;height:22px;padding:3px;border:0;border-radius:99px;background:#cbd5e1;cursor:pointer}.settings-toggle span{display:block;width:16px;height:16px;border-radius:50%;background:#fff;transition:transform .2s}.settings-toggle.on{background:#2868c7}.settings-toggle.on span{transform:translateX(17px)}.settings-theme-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.settings-theme,.settings-button,.settings-primary{display:inline-flex;align-items:center;justify-content:center;gap:7px;padding:9px 12px;border-radius:7px;font-weight:800;cursor:pointer}.settings-theme{display:grid;justify-content:stretch;border:1px solid #e3e9f1;background:#fff;color:#536176;text-align:left}.settings-theme.selected,.settings-button.selected{border-color:#2868c7;background:#eaf2ff;color:#2868c7}.theme-preview{height:30px;border-radius:5px;background:#f2f5f9}.theme-preview.dark{background:#1f2b40}.theme-preview.system{background:linear-gradient(135deg,#f2f5f9 50%,#1f2b40 50%)}.settings-button{border:1px solid #e3e9f1;background:#fff;color:#536176}.settings-primary{border:1px solid #2868c7;background:#2868c7;color:#fff}.settings-choices,.settings-actions{display:flex;gap:8px}.settings-choices{margin-top:10px}.settings-actions{justify-content:flex-end}.settings-message{display:flex;justify-content:space-between;padding:11px;border:1px solid #cfe0f9;border-radius:8px;background:#f2f7ff;color:#315c94;font-size:12px}.settings-message button{border:0;background:transparent;color:inherit;cursor:pointer}@media(max-width:760px){.settings-layout{grid-template-columns:1fr}.settings-tabs{display:flex;overflow-x:auto}.settings-tab{white-space:nowrap}.settings-grid,.settings-theme-grid{grid-template-columns:1fr}}@media(max-width:480px){.settings-card{padding:16px}.settings-profile{align-items:flex-start;flex-direction:column}.settings-actions{flex-direction:column-reverse}.settings-actions button{width:100%}}`;

function Toggle({ enabled, onChange }) { return <button type="button" className={`settings-toggle ${enabled ? "on" : ""}`} role="switch" aria-checked={enabled} onClick={() => onChange(!enabled)}><span /></button>; }
function Field({ label, value, type = "text", disabled = false, onChange }) { return <label className="settings-field"><span>{label}</span><input type={type} value={value} disabled={disabled} onChange={onChange ? (event) => onChange(event.target.value) : undefined} /></label>; }

function Settings() {
  const [activeTab, setActiveTab] = useState("profile");
  const [message, setMessage] = useState("");
  const [editing, setEditing] = useState(false);
  const [theme, setTheme] = useState("Light mode");
  const [fontSize, setFontSize] = useState("Medium");
  const [profile, setProfile] = useState({ name: "Admin User", email: "admin@lcci.edu.ph", phone: "+63 917 123 4567" });
  const [password, setPassword] = useState({ current: "", next: "", confirm: "" });
  const [notifications, setNotifications] = useState(Object.fromEntries(notificationItems.map((label) => [label, true])));
  const notify = (text) => setMessage(text);
  const updateProfile = (key, value) => setProfile((current) => ({ ...current, [key]: value }));
  const updatePassword = (key, value) => setPassword((current) => ({ ...current, [key]: value }));
  return <main className="settings-page"><style>{styles}</style><header className="settings-heading"><h1>System Settings</h1><p>Manage your account preferences and system configuration.</p></header><div className="settings-layout"><nav className="settings-tabs" aria-label="Settings sections">{tabs.map(([id, label, Icon]) => <button type="button" className={`settings-tab ${activeTab === id ? "active" : ""}`} key={id} onClick={() => setActiveTab(id)}><Icon size={16} />{label}</button>)}</nav><div className="settings-content">
    {activeTab === "profile" && <section className="settings-card"><div className="settings-card-heading"><div><h2>Profile settings</h2><p>Keep your LCCI account information up to date.</p></div><button type="button" className="settings-button" onClick={() => setEditing((current) => !current)}>{editing ? "Done editing" : "Edit profile"}</button></div><div className="settings-profile"><span className="settings-avatar">AU</span><div><strong>{profile.name}</strong><span>OJT Coordination Office</span></div></div><div className="settings-grid"><Field label="Full name" value={profile.name} disabled={!editing} onChange={(value) => updateProfile("name", value)} /><Field label="Email address" value={profile.email} disabled={!editing} onChange={(value) => updateProfile("email", value)} /><Field label="Contact number" value={profile.phone} disabled={!editing} onChange={(value) => updateProfile("phone", value)} /><Field label="Role" value="Administrator" disabled /></div></section>}
    {activeTab === "account" && <section className="settings-card"><div className="settings-card-heading"><div><h2>Account settings</h2><p>Update your password and account access.</p></div><LockKeyhole size={20} color="#2868c7" /></div><div className="settings-grid"><Field label="Current password" type="password" value={password.current} onChange={(value) => updatePassword("current", value)} /><Field label="New password" type="password" value={password.next} onChange={(value) => updatePassword("next", value)} /><Field label="Confirm new password" type="password" value={password.confirm} onChange={(value) => updatePassword("confirm", value)} /></div><button type="button" className="settings-primary" style={{ marginTop: 16 }} onClick={() => notify("Password update is ready to connect to authentication.")}>Update password</button><div className="settings-row"><div><strong>Two-factor authentication</strong><p>Add another layer of protection to your account.</p></div><Toggle enabled={notifications.security ?? false} onChange={(value) => setNotifications((current) => ({ ...current, security: value }))} /></div></section>}
    {activeTab === "notifications" && <section className="settings-card"><div className="settings-card-heading"><div><h2>Notification settings</h2><p>Choose which updates you want to receive.</p></div><Bell size={20} color="#2868c7" /></div>{notificationItems.map((label) => <div className="settings-row" key={label}><div><strong>{label}</strong><p>Manage {label.toLowerCase()} for your account.</p></div><Toggle enabled={notifications[label]} onChange={(value) => setNotifications((current) => ({ ...current, [label]: value }))} /></div>)}</section>}
    {activeTab === "preferences" && <section className="settings-card"><div className="settings-card-heading"><div><h2>Appearance and system preferences</h2><p>Personalize your dashboard experience.</p></div><Palette size={20} color="#2868c7" /></div><div className="settings-theme-grid">{[["Light mode", Sun, ""], ["Dark mode", Moon, "dark"], ["System default", Palette, "system"]].map(([label, Icon, preview]) => <button type="button" className={`settings-theme ${theme === label ? "selected" : ""}`} key={label} onClick={() => setTheme(label)}><span className={`theme-preview ${preview}`} /><span><Icon size={14} /> {label}</span></button>)}</div><strong style={{ display: "block", marginTop: 18, fontSize: 11, color: "#718096" }}>Font size</strong><div className="settings-choices">{["Small", "Medium", "Large"].map((size) => <button type="button" className={`settings-button ${fontSize === size ? "selected" : ""}`} key={size} onClick={() => setFontSize(size)}>{size}</button>)}</div><div className="settings-grid"><Field label="Language" value="English" disabled /><Field label="Date format" value="MMM DD, YYYY" disabled /><Field label="Time format" value="12-hour" disabled /><Field label="Default dashboard page" value="Admin dashboard" disabled /></div></section>}
    {activeTab === "security" && <section className="settings-card"><div className="settings-card-heading"><div><h2>Security and privacy</h2><p>Review access and manage your information.</p></div><ShieldCheck size={20} color="#2868c7" /></div><div className="settings-row"><div><strong>Last login</strong><p>September 5, 2026 at 1:12 PM from this device</p></div><Check size={18} color="#16866f" /></div><div className="settings-row"><div><strong>Active sessions</strong><p>1 active session on Windows device</p></div><button type="button" className="settings-button" onClick={() => notify("Login activity is ready to connect.")}>View login activity</button></div><div className="settings-row"><div><strong>Log out from all devices</strong><p>End all other active sessions.</p></div><button type="button" className="settings-button" onClick={() => notify("All other sessions would be signed out.")}><LogOut size={14} /> Sign out all</button></div><div className="settings-row"><div><strong>Download my data</strong><p>Export a copy of your profile and OJT records.</p></div><button type="button" className="settings-button" onClick={() => notify("Your data export has been requested.")}><Download size={14} /> Download</button></div><div className="settings-row"><div><strong>Privacy preferences</strong><p>Review how your information is used.</p></div><button type="button" className="settings-button" onClick={() => notify("Privacy preferences are ready to configure.")}>Manage</button></div></section>}
    <div className="settings-actions"><button type="button" className="settings-button" onClick={() => notify("Changes were cancelled.")}>Cancel</button><button type="button" className="settings-primary" onClick={() => notify("Your settings have been saved successfully.")}><Save size={15} /> Save changes</button></div>{message && <div className="settings-message"><span>{message}</span><button type="button" aria-label="Dismiss message" onClick={() => setMessage("")}><X size={15} /></button></div>}
  </div></div></main>;
}

export default Settings;
